[package]
name = "wat-core"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib"]

[features]
default = []
native-fs = []
native-proc = []
native-pty = ["dep:portable-pty"]

[dependencies]
portable-pty = { version = "0.8", optional = true }

[dev-dependencies]
insta = "1"
proptest = "1"
